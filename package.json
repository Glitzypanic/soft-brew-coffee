{
  "name": "soft-brew-coffee",
  "version": "1.0.0",
  "description": "Soft Brew Coffee — landing editorial de cold brew de especialidad",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1 --port 4322",
    "check": "astro check",
    "build": "astro build",
    "preview": "astro preview --host 127.0.0.1 --port 4322",
    "test": "playwright test",
    "format": "prettier --write src tests *.ts *.mjs",
    "verify": "npm run check && npm test"
  },
  "type": "module",
  "dependencies": {
    "@fontsource-variable/inter": "^5.3.0",
    "@fontsource/jetbrains-mono": "^5.3.0",
    "astro": "^7.3.1",
    "motion": "^13.2.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.63.0",
    "@types/node": "^26.4.1",
    "agent-browser": "^0.36.0",
    "prettier": "^3.9.6",
    "prettier-plugin-astro": "^0.14.1",
    "typescript": "^6.0.3"
  },
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  }
}
